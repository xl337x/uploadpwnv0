requests>=2.28
beautifulsoup4>=4.11
pyotp>=2.8
Pillow>=9.0

# optional — only needed for specific features
# selenium>=4.10        # JS-heavy login pages (--login-method selenium)
# requests-ntlm>=1.2    # --ntlm-auth
